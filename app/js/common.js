$(function () {
    // $('[data-action=open-modal]').click(function() {
    //     const modal = $(`${$(this).attr('data-modal')}.modal`);
    //     $(modal).toggleClass('active');
    // });

    // $('.modal').click(function(e) {
    //     if ($('.modal-inner-block').has(e.target).length < 1) {
    //         $('.modal').removeClass('active');
    //     }
    // });

    // $('[data-action=modal-close]').click(function() {
    //     $('.modal').removeClass('active');
    // });
});